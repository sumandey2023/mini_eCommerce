<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sport Sphere</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Sour+Gummy:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Sour Gummy", serif;
      }
    </style>
  </head>
  <body>
    <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %> <% if
    (loggedin) { %>
    <nav
      class="fixed top-0 left-0 w-full flex items-center justify-between px-10 py-4 bg-gradient-to-r from-purple-500 to-pink-500 shadow-lg z-40"
    >
      <h2 class="text-white text-2xl font-bold">Sport Sphere</h2>
      <div class="hidden md:flex items-center gap-6">
        <a class="text-white hover:text-gray-300" href="/shop">Home</a>
        <a class="text-white hover:text-gray-300" href="#" id="about-nav"
          >About</a
        >
        <a class="text-white hover:text-gray-300" href="/cart">Cart</a>
        <a class="text-white hover:text-gray-300" href="/profile">Profile</a>
        <a
          class="px-4 py-2 bg-white text-pink-500 rounded-md"
          href="user/logout"
          >Logout</a
        >
      </div>
      <button id="menu-toggle" class="md:hidden text-white text-2xl">☰</button>
    </nav>

    <!-- Mobile Menu -->
    <div
      id="mobile-menu"
      class="fixed top-0 right-0 w-3/4 h-full bg-gradient-to-r from-purple-500 to-pink-500 text-white py-4 transform translate-x-full transition-transform duration-300 z-50"
    >
      <button id="close-menu" class="absolute top-4 left-4 text-white text-2xl">
        &times;
      </button>
      <div class="flex flex-col items-start px-6 mt-10">
        <a class="py-2" href="/shop">Home</a>
        <a class="py-2" href="#" id="about-nav-mobile">About</a>
        <a class="py-2" href="/cart">Cart</a>
        <a class="py-2" href="/profile">Profile</a>
        <a
          class="py-2 bg-white text-pink-500 px-4 rounded-md"
          id="logout"
          href="/logout"
          >Logout</a
        >
      </div>
    </div>
    <% } %>
    <div
      id="about-popup"
      class="fixed top-0 left-0 w-full h-full bg-black bg-opacity-50 flex items-center justify-center hidden"
    >
      <div class="bg-white p-6 rounded-lg shadow-lg w-11/12 md:w-1/2 relative">
        <button
          id="close-about"
          class="absolute top-3 right-3 text-xl text-gray-600 hover:text-gray-800"
        >
          &times;
        </button>
        <h2 class="text-2xl font-semibold mb-3">About Us</h2>

        <p class="text-gray-700 mb-4">
          Hi, I’m Suman Dey, the founder of Sport Sphere – your one-stop shop
          for all things sports! 🏆 At Sport Sphere, we believe that quality
          sports gear should be accessible to everyone. Whether you're an
          athlete, a fitness enthusiast, or just starting your journey, we’ve
          got the best products at affordable prices. Our goal is to provide
          high-quality sports products that enhance performance, comfort, and
          style. We are passionate about helping you stay active, competitive,
          and motivated. Join us in celebrating the spirit of sports and making
          every game a great one! Stay connected with us and be part of our
          growing sports community. 🚀
        </p>

        <div class="flex gap-4">
          <a href="#" class="text-blue-600 text-2xl hover:text-blue-800">
            <i class="ri-facebook-circle-fill"></i>
          </a>
          <a href="#" class="text-gray-800 text-2xl hover:text-gray-900">
            <i class="ri-github-fill"></i>
          </a>
          <a href="#" class="text-blue-500 text-2xl hover:text-blue-700">
            <i class="ri-linkedin-box-fill"></i>
          </a>
          <a href="#" class="text-green-500 text-2xl hover:text-green-700">
            <i class="ri-whatsapp-fill"></i>
          </a>
        </div>
      </div>
    </div>
    <script>
      // Mobile Menu Toggle
      const menuToggle = document.getElementById("menu-toggle");
      const mobileMenu = document.getElementById("mobile-menu");
      const closeMenu = document.getElementById("close-menu");
      const aboutNavMobile = document.getElementById("about-nav-mobile");

      menuToggle.addEventListener("click", () => {
        mobileMenu.classList.toggle("translate-x-full");
      });

      closeMenu.addEventListener("click", () => {
        mobileMenu.classList.add("translate-x-full");
      });

      // About Popup
      const aboutPopup = document.getElementById("about-popup");
      const closeAbout = document.getElementById("close-about");
      const aboutNav = document.getElementById("about-nav");

      function openAboutPopup() {
        aboutPopup.classList.remove("hidden");
      }

      function closeAboutPopup() {
        aboutPopup.classList.add("hidden");
      }

      aboutNav.addEventListener("click", openAboutPopup);
      closeAbout.addEventListener("click", closeAboutPopup);

      // Close mobile menu before opening About popup
      aboutNavMobile.addEventListener("click", () => {
        mobileMenu.classList.add("translate-x-full"); // Close menu
        setTimeout(openAboutPopup, 300); // Open About popup after transition
      });
    </script>
  </body>
</html>
